1.	Проанализировать структуру БД vk, которую мы создали на занятии, и внести предложения по усовершенствованию (если такие идеи есть). 

-По структуре все понятно.

2.	Добавить необходимую таблицу/таблицы для того, чтобы можно было использовать лайки для медиа файлов, постов и пользователей.

Добавлены 
-- Таблица постов
-- Таблица типов likes
-- Таблица лайков
-- Таблица содержимого лайков.
Таблица типов лайков содержит типы содержимого. На данный момент 3 строки: image, user, media. Процесс дополнения новых видов лайков сводится к добавлению новой строки в типы. Контент лайк сделан в отдельную таблицу. Поле контента content_id. В данном случаи процесс заполнения должен учитывать, что выбор происходит из таблиц медиа и таблиц пост. http://filldb.info  не позволяет генерировать подобное заполнение поэтому в базе заполнено по таблице пост. 

3.	Используя сервис http://filldb.info или другой по вашему желанию, сгенерировать тестовые данные для всех таблиц, учитывая логику связей. Для всех таблиц, где это имеет смысл, создать не менее 100 строк. Создать локально БД vk и загрузить в неё тестовые данные.

- В приложенном файле Full_Data.sql.dmp заполненая база данных
